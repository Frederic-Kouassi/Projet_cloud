services:
  dotolist_service:
    build: .
    container_name: dotolist_service
    command: gunicorn Dotolist.wsgi:application --bind 0.0.0.0:8000 --timeout 120
    volumes:
      - .:/app
      - ./staticfiles:/app/staticfiles
      - ./media:/app/media
    ports:
      - "8007:8000"
    env_file:
      - .env
    depends_on:
      - dotolist_db
    networks:
      - mynetwork

  dotolist_db:
    image: postgres:15
    container_name: dotolist_db
    restart: always
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5436:5432"
    networks:
      - mynetwork

volumes:
  postgres_data:

networks:
  mynetwork:
    driver: bridge